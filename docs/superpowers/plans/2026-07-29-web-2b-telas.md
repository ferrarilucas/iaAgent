# apps/web — Plano 2b (Shell + Espaços + Transações) — Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sobre a auth já pronta, entregar a casca autenticada do painel (nav) + a tela de Espaços & convites (ver membros, convidar por número com aviso no WhatsApp, aceitar convite) + a tela de Transações (listar com filtros).

**Architecture:** Server Components e Server Actions do Next.js consumindo `packages/db` direto. Um helper `requireContext()` resolve, a partir da sessão do Better Auth, o `userId` e o `spaceId` do usuário. O convite avisa o convidado no WhatsApp via `packages/whatsapp` (gateway compartilhado — o painel não precisa do agente pra isso).

**Tech Stack:** Next.js (App Router, server actions), TypeScript strict, Tailwind, `@ia/db`, `@ia/whatsapp`, Better Auth (sessão), Vitest (só a função nova de repo).

## Global Constraints

- **NUNCA adicionar comentários no código** — nenhum `.ts`/`.tsx`.
- **TypeScript strict.** Dinheiro é string; nunca coagir `amount` para número.
- **Toda página/rota sob `/app` exige sessão** — usar `requireContext()` (redireciona pra `/login` se não logado ou sem espaço).
- **Envio ao WhatsApp só via `packages/whatsapp`** (`sendText`), com config de env.
- **Não desfazer a auth do 2a** (tabela `users` única, Better Auth adotando ela).
- **Verificação das partes web:** `pnpm --filter @ia/web typecheck` e o build (com env placeholder exportado, pois `env.ts` parseia no load) limpos. A função nova de repo tem teste PGlite.
- **Env placeholder pro build** (env.ts parseia no module load):
  `DATABASE_URL="postgresql://ia:ia@localhost:5432/ia_agent" BETTER_AUTH_SECRET="dev" BETTER_AUTH_URL="http://localhost:3000" EVOLUTION_API_URL="https://evo.example" EVOLUTION_INSTANCE="inst" EVOLUTION_API_KEY="key"`
- Commits frequentes, um por tarefa.

---

## Estrutura de arquivos

```
packages/db/
  src/repository/categories.ts        + listCategoriesForSpace
  test/categories.test.ts             + teste
apps/web/src/
  lib/session.ts                      requireContext()
  app/app/layout.tsx                  + nav (Espacos, Transacoes)
  app/app/page.tsx                    (ja existe; mantem placeholder)
  app/app/espacos/page.tsx            server component
  app/app/espacos/actions.ts          server actions (convidar, aceitar)
  app/app/transacoes/page.tsx         server component (lista + filtros)
```

---

### Task 1: `listCategoriesForSpace` no `packages/db`

As telas precisam listar categorias (nomes) para filtros e para resolver `category_id -> nome`.

**Files:**
- Modify: `packages/db/src/repository/categories.ts`
- Modify: `packages/db/test/categories.test.ts`

**Interfaces:**
- Produces: `listCategoriesForSpace(db, spaceId): Promise<Array<{ id: string; name: string; type: "despesa" | "receita" }>>`

- [ ] **Step 1: Adicionar o teste em `packages/db/test/categories.test.ts`**

```ts
import { listCategoriesForSpace } from "../src/repository/categories";

it("lista as categorias do espaco com id, nome e tipo", async () => {
  const t = await createTestDb(); close = t.close;
  const { space } = await bootstrapUser(t.db, { whatsappNumber: "5599", name: "L" });
  await seedCategories(t.db, space.id);
  const cats = await listCategoriesForSpace(t.db, space.id);
  const nomes = cats.map((c) => c.name);
  expect(nomes).toContain("alimentacao");
  expect(cats.every((c) => c.id && (c.type === "despesa" || c.type === "receita"))).toBe(true);
});
```
(reaproveita os imports já existentes no arquivo — `createTestDb`, `bootstrapUser`, `seedCategories`.)

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm --filter @ia/db test categories`
Expected: FAIL — `listCategoriesForSpace` não existe.

- [ ] **Step 3: Implementar em `packages/db/src/repository/categories.ts`**

```ts
export async function listCategoriesForSpace(
  db: Db,
  spaceId: string,
): Promise<Array<{ id: string; name: string; type: "despesa" | "receita" }>> {
  const rows = await db
    .select({ id: categories.id, name: categories.name, type: categories.type })
    .from(categories)
    .where(eq(categories.spaceId, spaceId));
  return rows.map((r) => ({ id: r.id, name: r.name, type: r.type }));
}
```
(usa `eq` e `categories` já importados no arquivo.)

- [ ] **Step 4: Rodar e ver passar + suíte**

Run: `pnpm --filter @ia/db test && pnpm --filter @ia/db typecheck`
Expected: toda a suíte passa; typecheck limpo.

- [ ] **Step 5: Commit**

```bash
git add packages/db/src/repository/categories.ts packages/db/test/categories.test.ts
git commit -m "feat(db): listCategoriesForSpace"
```

---

### Task 2: `requireContext()` + casca autenticada (nav)

**Files:**
- Create: `apps/web/src/lib/session.ts`
- Modify: `apps/web/src/app/app/layout.tsx`

**Interfaces:**
- Consumes: `auth` (lib/auth), `db` (lib/db), `getSpaceForUser` (@ia/db), `headers`/`redirect` (next).
- Produces: `requireContext(): Promise<{ userId: string; spaceId: string; userName: string | null; phoneNumber: string | null }>` — redireciona a `/login` se não houver sessão/espaço.

- [ ] **Step 1: Criar `apps/web/src/lib/session.ts`**

```ts
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { getSpaceForUser } from "@ia/db";
import { auth } from "./auth";
import { db } from "./db";

export async function requireContext() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) redirect("/login");
  const space = await getSpaceForUser(db, session.user.id);
  if (!space) redirect("/login");
  return {
    userId: session.user.id,
    spaceId: space.id,
    userName: session.user.name ?? null,
    phoneNumber: (session.user as { phoneNumber?: string | null }).phoneNumber ?? null,
  };
}
```
Reconciliação: se o campo `phoneNumber` não estiver no tipo da sessão do Better Auth instalado, leia-o do banco (`getUserByWhatsappNumber`/uma busca por id) — o objetivo é ter o número do usuário para listar convites pendentes. Registre o ajuste.

- [ ] **Step 2: Substituir `apps/web/src/app/app/layout.tsx` pela casca com nav**

```tsx
import type { ReactNode } from "react";
import Link from "next/link";
import { requireContext } from "@/lib/session";

export default async function AppLayout({ children }: { children: ReactNode }) {
  const ctx = await requireContext();
  return (
    <div className="min-h-screen">
      <header className="flex items-center justify-between border-b p-4">
        <span className="font-bold">Pilinha</span>
        <nav className="flex gap-4 text-sm">
          <Link href="/app">Inicio</Link>
          <Link href="/app/transacoes">Transacoes</Link>
          <Link href="/app/espacos">Espacos</Link>
        </nav>
        <span className="text-sm text-gray-500">{ctx.userName ?? ""}</span>
      </header>
      <main className="mx-auto max-w-4xl p-4">{children}</main>
    </div>
  );
}
```

- [ ] **Step 3: Typecheck + build**

Run: (com env placeholder exportado) `pnpm --filter @ia/web typecheck && <env placeholder> pnpm --filter @ia/web build`
Expected: limpo; rotas continuam válidas.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/lib/session.ts apps/web/src/app/app/layout.tsx
git commit -m "feat(web): requireContext e casca autenticada com nav"
```

---

### Task 3: Tela de Espaços & convites

**Files:**
- Create: `apps/web/src/app/app/espacos/page.tsx`
- Create: `apps/web/src/app/app/espacos/actions.ts`

**Interfaces:**
- Consumes: `requireContext`, `db`, `@ia/db` (`getSpaceMembers`, `createInvitation`, `getPendingInvitationsForNumber`, `acceptInvitation`), `@ia/whatsapp` (`sendText`), `env`.
- Produces: a tela; as actions `convidar(formData)` e `aceitar(formData)`.

- [ ] **Step 1: Criar as server actions `apps/web/src/app/app/espacos/actions.ts`**

```ts
"use server";
import { revalidatePath } from "next/cache";
import { createInvitation, acceptInvitation } from "@ia/db";
import { sendText } from "@ia/whatsapp";
import { requireContext } from "@/lib/session";
import { db } from "@/lib/db";
import { env } from "@/env";

const evolution = {
  evolutionApiUrl: env.EVOLUTION_API_URL,
  evolutionInstance: env.EVOLUTION_INSTANCE,
  evolutionApiKey: env.EVOLUTION_API_KEY,
};

export async function convidar(formData: FormData) {
  const ctx = await requireContext();
  const numero = String(formData.get("numero") ?? "").trim();
  if (!numero) return;
  await createInvitation(db, { spaceId: ctx.spaceId, invitedBy: ctx.userId, invitedNumber: numero });
  await sendText(evolution, numero, "Voce foi convidado para compartilhar as contas no Pilinha. Abra o painel para aceitar.").catch(() => {});
  revalidatePath("/app/espacos");
}

export async function aceitar(formData: FormData) {
  const ctx = await requireContext();
  const id = String(formData.get("id") ?? "");
  if (!id) return;
  await acceptInvitation(db, id, ctx.userId);
  revalidatePath("/app/espacos");
}
```
Nota: `sendText` lança em erro HTTP; o `.catch(() => {})` mantém o convite mesmo se o aviso falhar (o convite vale pelo painel).

- [ ] **Step 2: Criar a página `apps/web/src/app/app/espacos/page.tsx`**

```tsx
import { requireContext } from "@/lib/session";
import { db } from "@/lib/db";
import { getSpaceMembers, getPendingInvitationsForNumber } from "@ia/db";
import { convidar, aceitar } from "./actions";

export default async function EspacosPage() {
  const ctx = await requireContext();
  const membros = await getSpaceMembers(db, ctx.spaceId);
  const convites = ctx.phoneNumber ? await getPendingInvitationsForNumber(db, ctx.phoneNumber) : [];

  return (
    <div className="flex flex-col gap-8">
      <section>
        <h1 className="text-xl font-bold">Membros do espaco</h1>
        <ul className="mt-2">
          {membros.map((m) => (
            <li key={m.userId} className="flex justify-between border-b py-2">
              <span>{m.name ?? "Sem nome"}</span>
              <span className="text-gray-500">{m.role}</span>
            </li>
          ))}
        </ul>
      </section>

      <section>
        <h2 className="text-lg font-semibold">Convidar</h2>
        <form action={convidar} className="mt-2 flex gap-2">
          <input name="numero" placeholder="Numero (ex: 5511999999999)" className="flex-1 rounded border p-2" />
          <button className="rounded bg-black px-4 text-white">Convidar</button>
        </form>
      </section>

      {convites.length > 0 ? (
        <section>
          <h2 className="text-lg font-semibold">Convites recebidos</h2>
          <ul className="mt-2">
            {convites.map((c) => (
              <li key={c.id} className="flex items-center justify-between border-b py-2">
                <span>Convite para um espaco</span>
                <form action={aceitar}>
                  <input type="hidden" name="id" value={c.id} />
                  <button className="rounded bg-green-600 px-3 py-1 text-white">Aceitar</button>
                </form>
              </li>
            ))}
          </ul>
        </section>
      ) : null}
    </div>
  );
}
```

- [ ] **Step 3: Typecheck + build**

Run: `pnpm --filter @ia/web typecheck && <env placeholder> pnpm --filter @ia/web build`
Expected: build lista `/app/espacos`; typecheck limpo. Reconcilie campos/nomes se algum tipo do `@ia/db` divergir.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/app/app/espacos
git commit -m "feat(web): tela de espacos e convites"
```

---

### Task 4: Tela de Transações (lista + filtros)

**Files:**
- Create: `apps/web/src/app/app/transacoes/page.tsx`

**Interfaces:**
- Consumes: `requireContext`, `db`, `@ia/db` (`listTransactionsForSpace`, `listCategoriesForSpace`, `getSpaceMembers`).
- Produces: a tela com filtros por período e tipo (via querystring).

- [ ] **Step 1: Criar `apps/web/src/app/app/transacoes/page.tsx`**

```tsx
import { requireContext } from "@/lib/session";
import { db } from "@/lib/db";
import { listTransactionsForSpace, listCategoriesForSpace, getSpaceMembers } from "@ia/db";

type Search = { from?: string; to?: string; type?: string };

export default async function TransacoesPage({ searchParams }: { searchParams: Promise<Search> }) {
  const ctx = await requireContext();
  const sp = await searchParams;
  const type = sp.type === "receita" || sp.type === "despesa" ? sp.type : undefined;
  const [txs, cats, membros] = await Promise.all([
    listTransactionsForSpace(db, ctx.spaceId, { from: sp.from, to: sp.to, type }),
    listCategoriesForSpace(db, ctx.spaceId),
    getSpaceMembers(db, ctx.spaceId),
  ]);
  const catName = new Map(cats.map((c) => [c.id, c.name]));
  const memberName = new Map(membros.map((m) => [m.userId, m.name ?? "?"]));

  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-xl font-bold">Transacoes</h1>
      <form className="flex flex-wrap gap-2" method="get">
        <input type="date" name="from" defaultValue={sp.from ?? ""} className="rounded border p-2" />
        <input type="date" name="to" defaultValue={sp.to ?? ""} className="rounded border p-2" />
        <select name="type" defaultValue={type ?? ""} className="rounded border p-2">
          <option value="">Todos</option>
          <option value="despesa">Despesa</option>
          <option value="receita">Receita</option>
        </select>
        <button className="rounded bg-black px-4 text-white">Filtrar</button>
      </form>
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b text-left text-gray-500">
            <th className="py-2">Data</th><th>Tipo</th><th>Categoria</th><th>Descricao</th><th>Quem</th><th className="text-right">Valor</th>
          </tr>
        </thead>
        <tbody>
          {txs.map((t) => (
            <tr key={t.id} className="border-b">
              <td className="py-2">{t.occurredAt}</td>
              <td>{t.type}</td>
              <td>{t.categoryId ? catName.get(t.categoryId) ?? "-" : "-"}</td>
              <td>{t.description ?? ""}</td>
              <td>{memberName.get(t.createdBy) ?? "?"}</td>
              <td className="text-right">R$ {t.amount}</td>
            </tr>
          ))}
        </tbody>
      </table>
      {txs.length === 0 ? <p className="text-gray-500">Nenhum lancamento no filtro.</p> : null}
    </div>
  );
}
```
Reconciliação: em Next 15 `searchParams` é uma Promise em Server Components; se a versão instalada diferir, ajuste o tipo/await mantendo os filtros.

- [ ] **Step 2: Typecheck + build**

Run: `pnpm --filter @ia/web typecheck && <env placeholder> pnpm --filter @ia/web build`
Expected: build lista `/app/transacoes`; typecheck limpo.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/app/transacoes
git commit -m "feat(web): tela de transacoes com filtros"
```

---

## Self-Review (feito pelo autor do plano)

**Cobertura (recorte 2b):**
- `listCategoriesForSpace` (pros filtros/nomes) → Task 1. ✔
- Sessão → contexto (`userId`/`spaceId`) + casca autenticada com nav → Task 2. ✔
- Espaços: ver membros, convidar (com aviso no WhatsApp via `packages/whatsapp`), aceitar convite → Task 3. ✔
- Transações: listar com filtros por período/tipo, com nomes de categoria e de quem lançou → Task 4. ✔
- Toda rota `/app/*` protegida por `requireContext` → Tasks 2-4. ✔

**Fora deste plano (2c):** Dashboard (gráficos Recharts) e Limites (CRUD + barra de progresso).

**Placeholders:** nenhum de escopo; onde a API do Next 15/Better Auth pode divergir (tipo de `searchParams`, campo `phoneNumber` na sessão), há instrução de reconciliação — inerente a libs externas. O código das telas e actions é concreto.

**Consistência de tipos:** `requireContext()` retorna `{ userId, spaceId, userName, phoneNumber }`, usado igual nas tasks; funções do `@ia/db` (`getSpaceMembers`, `createInvitation`, `getPendingInvitationsForNumber`, `acceptInvitation`, `listTransactionsForSpace`, `listCategoriesForSpace`) batem com as assinaturas já existentes/da Task 1; `sendText(evolution, numero, texto)` bate com `@ia/whatsapp`.

**Nota:** as telas web são verificadas por typecheck + build; a validação de runtime (sessão real, dados reais) acontece ao rodar o painel — o que também é o smoke test da auth do 2a que ficou pendente.
